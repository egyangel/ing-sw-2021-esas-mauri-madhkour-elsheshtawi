package it.polimi.ingsw;

import it.polimi.ingsw.model.EnumClasses.MarbleColor;
import it.polimi.ingsw.model.Marble;
import it.polimi.ingsw.model.MarketTray;

import java.util.ArrayList;

/**
 * Hello world!
 *
 */
public class App 
{


    public static void main( String[] args )
    {

        MarketTray test = new MarketTray();


        /*This block here I have to ask to the tutor, so don't change it

          Set<Integer> temp_position = new HashSet<Integer>();
          while(temp_position.size()<12) {

              temp_position.add((int) (13.00* Math.random()));
              System.out.println("pseudo random int between 1 and 10 : " +temp_position.toString());
          }

         for(int i = 0; i < 13; i++) {
            System.out.println(temp_position.get(i));
          }

          Iterator i;
          Object o;

          i = temp_position.iterator();

          while ( i.hasNext() ) {
              o = i.next();
              System.out.println(o.toString());
          }
        */
    }
}

